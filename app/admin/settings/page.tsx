import { createClient } from '@/lib/supabase/server'
import Link from 'next/link'
export default async function AdminSettings(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();return <div className="admin-shell"><div className="admin-card"><div className="eyebrow">SETTINGS</div><h1>Admin Settings</h1><p className="muted">Authenticated as {user?.email}</p><div style={{marginTop:22,padding:18,border:'1px solid var(--line)',borderRadius:12,background:'var(--panel-2)'}}><div className="detail-kicker">DATA BACKEND</div><div style={{marginTop:8,fontWeight:700}}>Supabase Postgres + Realtime</div><p className="muted">Public users can browse data. Only users with the admin role can write to the command database.</p></div><Link className="ghost-btn" style={{display:'inline-flex',marginTop:18}} href="/admin">Back</Link></div></div>}
