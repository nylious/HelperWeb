# V9 CHANGELOG

- Added an independent editable Header Icon setting in Admin -> Settings.
- Header icon now replaces the static command glyph in the public top navigation.
- Header icon uploads support PNG, SVG, JPG/JPEG and WebP (5 MB max).
- Added `header_icon_url` to `site_settings`.
- Added `supabase/upgrade_v9.sql`.
- Existing homepage logo remains a separate setting.
